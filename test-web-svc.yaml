apiVersion: v1
kind: Service
metadata:
  name: test-server-service
  labels:
    app: test-web
  namespace: test
spec:
  type: NodePort
  selector:
    app: test-web
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8080
      nodePort: 30088
